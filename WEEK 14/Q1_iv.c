#include <stdio.h>

int main()
{
    /*
    char *p = "abcd";
    while(*p)
    {
        putchar(*p++);
    }
    //prints 'abcd'
    */

    /*
    char *text = "abcd";
    char *p = text;
    
    p+= strlen(p) - 1;
    while(text <= p)
    {
        puts(p--);
    }
    */

    return 0;
}